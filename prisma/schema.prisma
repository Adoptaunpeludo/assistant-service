generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Documents {
  id      String                 @id @default(cuid())
  content String
  vector  Unsupported("vector")?
}
